plugins {
    id 'java'
    id 'application'
}

sourceSets {
    main.java.srcDirs = ['.']
}

// Solve problems with illegal characters on Windows with cp1250 codepage
compileJava.options.encoding = "UTF-8"

application {
    mainClass = 'FormulaTest'
}
